# Farmer Marketplace (Front-end only)

## How to run
1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`
3. Open the URL shown by Vite (usually http://localhost:5173)

This is a front-end-only React app (Vite). Data comes from `src/data/products.json` and cart is stored in localStorage.
